{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd backend && python -c \"import uvicorn, sys; sys.path.insert(0, '.'); from main import app; uvicorn.run(app, host='0.0.0.0', port=$PORT)\"",
    "healthcheckPath": "/api/status",
    "restartPolicyType": "ON_FAILURE"
  }
}
